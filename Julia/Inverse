# Inverse #
function invert(A)
    # input:    A square matrix 
    # output:   A^{-1}
    
    (a,b) = gaussianElimination(A, eye(m))
    
    (a1,b1) = upwardGaussianElimination(a,b)
    
    Ainv=(a1,b1)[2]
    
    # fill the details (rather short!)
    return Ainv
end
